export const CARGAR = "[geolocalizacion] CARGAR";

export const CARGAR_SUCCESS = "[geolocalizacion] CARGAR success";

export const CARGAR_ERROR = "[geolocalizacion] CARGAR error";

export const cargar = (optons) => ({
	type: CARGAR,
});
